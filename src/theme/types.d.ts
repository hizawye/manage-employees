import 'react-native-paper';

declare global {
  namespace ReactNativePaper {
    interface MD3Colors {
      success: string;
      present: string;
      absent: string;
      halfDay: string;
      border: string;
    }
  }
}
